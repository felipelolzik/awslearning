-Fix para Git

# Git aliases
alias gaa='git add -A'
alias gcm='git commit -m'
alias gco='git checkout'
alias gbr='git branch -r'
alias gpl='git pull'
alias gps='git push'
alias gc='git clone'
alias gst='git status'
º
fix() {
        gaa && gcm "$@" && gps
}

